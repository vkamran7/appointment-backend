spring:
  datasource:
    url: jdbc:mysql://localhost:3306/calendar_v2
    username: test_user
    password: Acer123acer!@#
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        id:
          new_generator_mappings: false
    show-sql: true
  sql:
    init:
      mode: always